# Todo-List

Simple App showing a basic usage of **CoreData** in iOS. Focuses on `NSFetchedResultsController` & `UITableViews` with the following operations:
* Adding Data
* Removing Data
* Editing Existing Data

Also focuses on modeling the `NSManagedObjectContext` in a dependency injection pattern.
